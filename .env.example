# OPENAI_API_KEY: Required for OpenAI API calls (server-side only, never exposed in client bundle).
OPENAI_API_KEY=""

# NODE_ENV: Optional runtime environment.
NODE_ENV=

# PORT: Optional HTTP listening port (default 8787).
PORT=8787
